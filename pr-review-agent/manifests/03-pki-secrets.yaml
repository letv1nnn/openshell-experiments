# PKI secrets are created dynamically by scripts/generate-certs.sh and scripts/deploy.sh.
# This file is a placeholder for reference only.
#
# Secrets created:
#   openshell-server-tls         — server cert (tls.crt), key (tls.key), CA (ca.crt)
#   openshell-server-client-ca   — client CA for mTLS (ca.crt)
#   openshell-sandbox-jwt-signing-secret — JWT signing key (key)
