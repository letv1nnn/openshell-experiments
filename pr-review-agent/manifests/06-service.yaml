apiVersion: v1
kind: Service
metadata:
  name: openshell
  namespace: openshell
  labels:
    app.kubernetes.io/name: openshell
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 8443
      targetPort: 8443
      protocol: TCP
  selector:
    app.kubernetes.io/name: openshell
    app.kubernetes.io/component: gateway
